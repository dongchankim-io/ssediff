# ssediff
A high-performance, enterprise-grade tool to compare and diff live Server-Sent Events (SSE) streams 1-to-1. Powered by a concurrent, memory-safe Go engine and an interactive React visual diff UI. Features real-time temporal alignment, zero-leak TTL eviction, and dynamic JSON path correlation. Completely dockerized.
